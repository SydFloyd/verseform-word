{
  "name": "verseform-word",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "validate:manifest": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/validate-manifest.ps1",
    "check": "npm run typecheck && npm run test && npm run build && npm run validate:manifest"
  },
  "dependencies": {
    "bible-tools": "0.2.4"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "@types/office-js": "1.0.607",
    "office-addin-dev-certs": "2.0.10",
    "typescript": "5.9.3",
    "vite": "6.4.3",
    "vitest": "3.2.7"
  }
}
