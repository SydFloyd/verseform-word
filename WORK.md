# Verseform for Word work state

Updated: 2026-09-05

## Now

- Stage: `VFW-000` through the English-only `VFW-030` detector-hardening slice are complete; `VFW-040` is the active release/Windows-host slice.
- Active item: `VFW-040` validates the same add-in in the newly installed Word for Windows host and prepares the production/distribution boundary. `D-013` keeps the narrow WordApi 1.7 non-atomic validation-to-replace limitation explicit, `D-014` makes hover best effort, `D-015` closes the pane lifecycle gate with a safe prior-ID refusal, `D-016` fixes the DBS/cache/attribution boundary, `D-017` defers multilingual work pending DBS approval and English-pilot feedback, `D-018` keeps Microsoft's pinned desktop sideload helper transient, and `D-019` owns the pilot-driven hover-preview/click-insert loop.
- Product name: **Verseform for Word**; repository name: `verseform-word`.
- Public repository: `https://github.com/SydFloyd/verseform-word`.
- Stewardship baseline: MIT-licensed under `SydFloyd`; keep the repository there while building and coordinate any later DBS mirror or transfer explicitly.
- Release breadth: keep the product English-only until DBS has tested and approved the direction and the English pilot user has supplied practical feedback; any later language still requires its own corpus and Word offset evidence.
- DBS permission: persistent catalog and passage caching is permitted for this add-in; implementation must still be bounded, local, clearable, and content-only.
- Host baseline: one Office.js task-pane application and add-in-only XML manifest target Word on the web first and Word desktop later. WordApi 1.7 and a connected Microsoft 365 subscription are required for native annotations.
- Trust baseline: detection is local; no Bible is bundled; preview/insertion may send only normalized coordinates to DBS; no document prose, telemetry, account, or crash upload leaves through Verseform.

## Last verified

- Microsoft documents Word task-pane add-ins as cross-platform across web and desktop hosts.
- The installed Office.js type contract exposes `onParagraphChanged` in WordApi 1.6 and native annotation insertion, hover, click, and removal events in WordApi 1.7. It marks annotation popup-action options/events as WordApi 1.8. Annotation state is ephemeral and requires a connected Microsoft 365 subscription.
- Microsoft's unified manifest remains preview-only for Word production add-ins, so this seed uses an add-in-only XML manifest.
- DBS's BrowserBible verse detector is MIT-licensed and offers a pure multilingual detector, but its broader DOM/popup system and permissive reference grammar do not automatically fit the Word correctness boundary. Only its static 66-book section-code table is incorporated for VFW-020 response validation under the preserved MIT notice; VFW-030 adopts no further detector code or data.
- VFW-010's local controller uses one returned annotation ID per candidate, a generation-guarded start/stop runtime, task-pane preview/Insert/clear-selection fallback, labeled non-Scripture test text, a full paragraph/source/offset/Created-state recheck, and one Word range-replace command. It intentionally makes no DBS request. Its browser ledger uses same-origin locking and reread/delta writes; locking must be available before Word events bind. It atomically reserves one of at most 256 opaque slots before a host create, then commits the returned ID before publishing it as actionable, so simultaneous panes cannot overwrite ownership or overbook capacity. The reservation is not a recoverable owner record: a crash after Word creates an annotation but before commit can still leave an unowned marker. While a pane is alive, exact annotations removed during a refresh/replacement/removal event remain in a runtime-only retired map and retain their opaque ledger ID; they are reactivated only after Word proves their ID, `Created` state, paragraph, UTF-16 range, and source match. The controller never evicts them to make capacity: new markers fail closed. Stop attempts active and retired exact-ID cleanup without aging either. On startup, even a confirmed exact delete remains in the ledger; only a later startup result that says the ID is missing ages it, up to eight sweeps, so Word Undo cannot be treated as impossible. After pane close the required range/source metadata is deliberately forgotten. If an opaque ID loaded at startup later activates, the pane calls it a previous temporary annotation and refuses to map, alter, or delete it; this makes a late resurrection distinguishable without guessing its range. A malformed browser record blocks without overwriting the ledger. It attempts host cleanup per annotation ID and fails closed if the host reports that old markers could not be cleared; it does not scan or delete annotations by appearance. This is not a claim that cleanup succeeds in Word on the web.
- The completed Word on the web walk proved delimiter detection after a leading UTF-16 surrogate pair, duplicate-reference activation, exact replacement, one-step Undo, same-runtime Undo resurrection repair, one close/reopen-before-Undo ordering, readable dark-theme rendering, and safe operation across two simultaneous documents and task panes. In the final cross-document pass, `Document 2` detected `John 1:1` while `Document 1` retained three references; returning to `Document 1` still opened the exact preview. Closing and reopening the second pane, then typing another delimiter, restored detection. A standalone browser rendered the unsupported-host message with disabled controls and focus on its live status. The final insert → close → reopen → Undo sequence restored `John 1:1` and activated a current safe preview; Word did not surface the older opaque ID in that run, while the deterministic prior-ID branch proves the explicit no-change refusal if it does. Trusted pointer movement over the annotation did not deliver Word's documented hover event, so `D-014` retains it only as a best-effort convenience; click and `Alt+Down` are the proved activation paths. Windows forced-colors and a screen reader remain release-host validation.
- VFW-020 now uses a strict direct-browser DBS ARC boundary. The local version-one content cache holds at most 65 entries/4 MiB, expires the catalog after 24 hours and chapters after seven days, and is user-clearable without removing the separate translation preference. Schema/size/origin/coordinate/cancellation and malformed-payload tests keep document prose outside both request and storage inputs. Recorded payload regressions cover provider newlines, glued punctuation, trailing section headings, wrong book/chapter keys (including numbered-book ambiguity), and the singleton empty-chapter sentinel. The static 66-book DBS section-code table is the only BrowserBible detector artifact incorporated and its upstream MIT license is preserved in `NOTICE.md`.
- The VFW-020 Word on the web walk loaded 1,313 authorized translations and selected NASB by default. Activating `John 3:16` fetched and displayed NASB with Lockman attribution; one action inserted passage, editable citation, and attribution, and one Undo restored the reference. Reopening used the local cache; clearing it caused a fresh DBS fetch. Switching to KJV fetched the KJV passage and Public Domain attribution. Selecting the authorized Genesis-only Amele translation produced a clear unavailable state while copied Word content remained unchanged. Activating an uncached NET request and immediately changing the paragraph to `John 3:16 changed` left no preview or replacement; the reference remained local and the document was restored to `John 3:16` with NASB selected after the proof.
- VFW-030 adds versioned English corpus v1 and programmatic coverage of all 66 full canonical names and every approved alias. It proves conservative fuzzy acceptance/rejection, strict chapter/verse/range bounds, duplicates, range punctuation, newlines and closing delimiters, URL/email/compact-identifier/numeric-prose exclusions, generated-citation exclusion, mixed Hebrew/emoji UTF-16 offsets around English references, and zero provider calls. Exact multiword names now require actual whitespace, `mailto:` references are excluded, and en/em dashes close prose only after whitespace so verse ranges do not activate early. Five warmed scans of a 100,000-unit paragraph remain below 250 ms. Existing controller tests retain observable coauthor/stale refusal; this is not an atomic coauthor guarantee or a multilingual claim.
- The local fake-host, Office-adapter-double, fake-browser-lock, and DOM-harness suite proves delimiter gating, UTF-16 offsets, hover duplicate-ID mapping without a request when Word delivers the event, insert/clear race refusal, stale/unknown/removed-ID refusal, simulated partial cleanup failure/retry, deferred-reservation stop refusal, forced-unload/reopen exact-ID recovery, cross-document missing-ID retention without stop aging, opaque prior-ID post-close refusal, bounded retry, malformed-ledger fail-closed behavior, Browser ownership reread-under-lock concurrent claims/interleaved deltas/final-slot capacity, same-runtime revalidation of selected and non-selected IDs from an Undo-resurrected paragraph marker set (including late activation and exact duplicate cleanup), created-state gateway gating, no controller network call, additive-handler teardown attempts, unsupported/readiness host copy, task-pane live-region/status-focus/cancel wiring, and light/dark/forced-color declarations. It proves only those simulated contracts—not visual forced-colors or screen-reader behavior, browser lock/storage support in every target host, recovery of lost post-close paragraph metadata, or that a coauthor cannot edit between the final check and the replace sync.
- The focused interaction suite passes all 49 Word/controller/UI cases. Four new cases prove a ready hover preview inserts on click without another request, one pending hover request is shared with click and inserts once, click without hover requests once and inserts, and a reference changed during that request is refused without replacement.
- Microsoft's Word event table records that paragraph changes are coauthor-triggered but annotation click, hover, and removal are not. Therefore those events cannot establish an atomic stale guard around `Range.insertText`. `D-013` records the owner's acceptance of this narrow WordApi 1.7 platform limitation; the final exact reread and all observable stale/refusal paths remain fail-closed, and no atomic coauthor guarantee is claimed.
- `npm audit --audit-level=high` reports zero vulnerabilities.
- `npm run check` passes: strict typecheck, all 75 tests (fourteen reference/freshness/corpus, forty-nine Word/controller/UI, and twelve Scripture/provider/cache), production build, the development manifest contract, and the production-asset/manifest fixture gate.
- The exact pushed VFW-030 commit `0b5baedddc06dd7f332b506e15b46770645f27b2` passed GitHub Actions run `33977609955`.
- VFW-040 now has a deterministic production-manifest builder and local release gate. The builder requires explicit credential-free HTTPS app/support values and publisher name, replaces all eight localhost app resources with one approved base, and validates the resulting WordApi 1.7, `ReadWriteDocument`, HTTPS, and no-localhost contract. The built task pane must retain the reviewed CSP and contain no development origin. A non-routable fixture passes without inventing the still-pending D-011 production values.
- Microsoft's official validator accepted the manifest schema, identifier, package/host detection, HTTPS sources, support URL, icons, resources, and acceptance-service checks but initially rejected the pre-release manifest version `0.1.0.0` as below its supported `1.0` floor. VFW-040 corrects the independent Office manifest version to `1.0.0.0`; the official validator was rerun and declared the corrected manifest valid. It must run again against the real production manifest.
- The pinned Microsoft desktop sideload helper successfully registered `manifest.xml`, started the existing HTTPS source on port 3000, and launched Word for Windows with `Word add-in 82d488fc-c5a7-4aa5-8133-d56c32ef9a69.docx`. The endpoint returned HTTP 200 and Word was running. The session was then explicitly unregistered and its server stopped. Native app control was unavailable to the agent, so the visible Word walking flow, forced-colors, and screen-reader observations are not yet claimed. Version 6.1.2 added 539 packages and eight high-severity development advisories when installed locally while the production graph remained clean; `D-018` therefore makes it an on-demand pinned command, not a retained dependency. The package was removed and the retained graph again reports zero vulnerabilities.
- After a clean Word process restart, the Windows host loaded the sideload document and the Verseform pane. The pilot confirmed desktop hover delivery and exact preview, then identified the trip to the pane's Insert button as unacceptable friction for repeated Scripture work. `D-019` changes the active flow to preview-only hover and guarded click/`Alt+Down` insertion. The live Vite pane is awaiting confirmation of that hot-reloaded behavior.

## Owner inputs before public release

- Choose production HTTPS hosting and log-retention ownership.
- Choose the support URL and Microsoft Marketplace publisher identity with DBS.
- Obtain DBS product testing/approval and English-pilot feedback before reopening multilingual scope.

## Next proof

- Confirm in the live Windows host that hover previews without mutation, one click inserts exactly once without the pane, Undo restores the reference, and `Alt+Down` follows the same guarded path. Then run the full gate and stop the sideload session. Repeat under Windows forced colors and a screen reader before public release. Production hosting/log retention, support URL, publisher identity, external Microsoft validation, and DBS approval remain explicit release gates. Never reframe `D-013` as an atomic coauthor guarantee, `D-014` as a promised hover path, `D-015` as recovery of forgotten post-close metadata, or the mixed-direction offset test as multilingual support.

## Handoff rule

Replace **Now** and **Last verified** as evidence changes. Keep detailed history in version control, not this file.
